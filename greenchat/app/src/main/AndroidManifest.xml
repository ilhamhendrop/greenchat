<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greensoft.greenchat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenChat">
        <activity android:name=".ui.activity.message.ChatLogActivity"
            android:theme="@style/Theme.GreenChat.NoActionBar"></activity>
        <activity
            android:name=".ui.activity.message.MessagesActivity"
            android:theme="@style/Theme.GreenChat.NoActionBar" />
        <activity
            android:name=".ui.activity.register.RegisterActivity"
            android:theme="@style/Theme.GreenChat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.login.LoginActivity"
            android:theme="@style/Theme.GreenChat.NoActionBar" />
        <activity
            android:name=".ui.activity.message.NewMessageActivity"
            android:theme="@style/Theme.GreenChat.NoActionBar" />
    </application>

</manifest>